<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.SST" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.SST" parent="Base.Theme.SST">
    </style>
    <style name="Theme.SST.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
    <style name="Theme.SST.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="Theme.SST.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>